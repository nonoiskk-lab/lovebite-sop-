# Supabase — required only once the DataService (src/lib/data-service.ts) is
# implemented against a real project. Phase-1 runs entirely in-memory without
# these set.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only; never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=
